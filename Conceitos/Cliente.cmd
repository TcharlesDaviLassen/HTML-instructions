
Nessa aula falaremos do Cliente, 
que na maioria das vezes é o  Browser,
ou no nosso caso, também pode ser o cURL. 
O Cliente é a entidade que dá inicio à toda comunicação com um pedido, fora em algumas poucas exceções.
Esses pedidos são feitos através de ações, que usam os métodos do HTTP, como GET, POST, PUT e DELETE, 
que vamos ver com mais detalhes em uma próxima aula.



# O Cliente é a entidade que dá inicio à toda comunicação com um pedido, fora em algumas poucas exceções

# "Cliente" = User Agent

# "cURL" = A um user Agent, Na maioria das vezes é O Browser

## Cliente faz pedidos como
# "GET" = é quando quer pedir algo
# "POST" = É quando quer enviar algum dado
# "PUT" = Atualizar os dados 
# "DELETE" =  deletar os dados 
